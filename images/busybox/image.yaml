versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: busybox
  - apko:
      config: configs/latest-glibc.apko.yaml
      extractTagsFrom:
        package: busybox
        prefix: glibc-
ref: ghcr.io/jdolitsky/apko-failures/busybox